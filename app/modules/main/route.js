/*
angular.module("Discitur",
    [])
    .config(function ($routeProvider) {
        $routeProvider.
            when('/Lesson/:LessonId', { controller: 'LessonCtrl', templateUrl: 'lesson/detail.html' }).
            otherwise({ redirectTo: '/' });
    });

    */